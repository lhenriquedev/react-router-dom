import { Outlet } from "react-router-dom";
import { Navbar } from "../components/Navbar";

export const Home = () => {
  return (
    <section className="section">
      <h2>Home</h2>
    </section>
  );
};
